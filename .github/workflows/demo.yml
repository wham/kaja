name: demo
on:
  workflow_dispatch:
  pull_request:

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Build Docker image
        run: docker build . -t kaja-dev:latest --build-arg RUN_TESTS=true

      - name: Start Docker container
        run: |
          docker run --name kaja-dev -d -p 41520:41520 \
            -v $PWD/workspace:/workspace \
            kaja-dev:latest

          # Wait for server to start
          echo "Waiting for server to start..."
          for i in {1..60}; do
            if docker logs kaja-dev 2>&1 | grep -q "Server started"; then
              echo "Server started!"
              break
            fi
            sleep 1
          done

      - name: Take demo video and screenshots
        id: demo
        run: |
          npm install playwright
          npx playwright install chromium --with-deps
          mkdir -p .github/demo
          node .github/scripts/take-demo.js

      - name: Convert video to GIF
        run: |
          sudo apt-get update && sudo apt-get install -y ffmpeg
          ffmpeg -i .github/demo/demo.webm -vf "fps=10,scale=960:-1:flags=lanczos" -c:v gif .github/demo/demo.gif
          rm .github/demo/demo.webm

      - name: Upload demo (artifact)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: demo
          path: .github/demo/

      - name: Commit and post demo
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR_NUMBER=${{ github.event.pull_request.number }}
          REPO="${{ github.repository }}"
          BRANCH="${{ github.head_ref }}"

          # Commit demo assets to PR branch
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .github/demo/
          git commit -m "Demo assets [skip ci]" || echo "No changes to commit"
          git push origin HEAD:${BRANCH}

          # Get the commit SHA for raw URLs
          COMMIT_SHA=$(git rev-parse HEAD)

          # Build URLs using raw.githubusercontent.com
          BASE_URL="https://raw.githubusercontent.com/${REPO}/${COMMIT_SHA}/.github/demo"
          GIF_URL="${BASE_URL}/demo.gif"
          HOME_URL="${BASE_URL}/home.png"
          CALL_URL="${BASE_URL}/call.png"
          COMPILER_URL="${BASE_URL}/compiler.png"

          # Build comment body
          BODY="## 🎬 Demo

![Demo](${GIF_URL})

<details>
<summary>Screenshots</summary>

### Home
![Home](${HOME_URL})

### Call
![Call](${CALL_URL})

### Compiler
![Compiler](${COMPILER_URL})"

          if [ -f .github/demo/newproject.png ]; then
            NEWPROJECT_URL="${BASE_URL}/newproject.png"
            BODY="${BODY}

### New Project
![New Project](${NEWPROJECT_URL})"
          fi

          BODY="${BODY}

</details>"

          # Update or create comment
          EXISTING_COMMENT=$(gh pr view ${PR_NUMBER} --json comments --jq '.comments[] | select(.body | startswith("## 🎬 Demo")) | .id' | head -1)

          if [ -n "$EXISTING_COMMENT" ]; then
            gh api repos/${REPO}/issues/comments/${EXISTING_COMMENT} -X PATCH -f body="$BODY"
          else
            gh pr comment ${PR_NUMBER} --body "$BODY"
          fi
