#!/bin/bash
set -e

# Post-build hook for wails desktop app
# Called from desktop/build/bin directory with ${bin} as argument
#
# Both protoc-go and protoc-gen-kaja are now embedded as Go libraries
# in the server binary, so there are no extra binaries to bundle.

echo "Post-build complete (no extra binaries to bundle)"
