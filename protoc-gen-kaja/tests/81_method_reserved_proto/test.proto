syntax = "proto3";
package test;

message Request {
  string data = 1;
}

message Response {
  string result = 1;
}

service TestService {
  // These method names need escaping
  rpc __proto__(Request) returns (Response);
  rpc _transport(Request) returns (Response);
}
