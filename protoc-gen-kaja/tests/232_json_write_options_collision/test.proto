syntax = "proto3";

package google.protobuf;

// Any triggers import of jsonWriteOptions from @protobuf-ts/runtime.
// A local message named jsonWriteOptions should cause a collision that needs aliasing.
message Any {
    string type_url = 1;
    bytes value = 2;
}

message jsonWriteOptions {
    string name = 1;
}
