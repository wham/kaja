syntax = "proto3";

package test;

// Message with a oneof named oneof_kind which collides with the
// oneofKindDiscriminator used by protobuf-ts for oneof unions.
message MyMessage {
    oneof oneof_kind {
        string text = 1;
        int32 number = 2;
    }
    string other = 3;
}
