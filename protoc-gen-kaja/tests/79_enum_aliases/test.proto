syntax = "proto3";

package test;

// Enum with aliases - multiple values with same number
enum Status {
  option allow_alias = true;
  UNKNOWN = 0;
  STARTED = 1;
  RUNNING = 1;  // alias for STARTED
  FINISHED = 2;
}

message Task {
  Status status = 1;
}
