syntax = "proto3";

// This comment is detached from package

package test;

message Request {
    string id = 1;
}

message Response {
    string result = 1;
}

service TestService {
    rpc DoSomething(Request) returns (Response);
}
