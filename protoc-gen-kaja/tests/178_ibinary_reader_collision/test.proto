syntax = "proto3";
package test;

// A message that collides with the runtime import type IBinaryReader.
message IBinaryReader {
    string name = 1;
    int32 level = 2;
}

message Container {
    IBinaryReader reader = 1;
}
