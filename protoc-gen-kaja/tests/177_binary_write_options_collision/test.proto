syntax = "proto3";
package test;

// A message that collides with the runtime import type BinaryWriteOptions.
message BinaryWriteOptions {
    string name = 1;
    int32 level = 2;
}

message Container {
    BinaryWriteOptions opts = 1;
}
