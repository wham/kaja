syntax = "proto3";
package google.protobuf;

// When Timestamp is present, the plugin imports PbLong from runtime.
// A local message named PbLong should cause a collision that needs aliasing.
message Timestamp {
    int64 seconds = 1;
    int32 nanos = 2;
}

message PbLong {
    string value = 1;
}
