syntax = "proto3";
package test;

// A message that collides with the runtime import type IBinaryWriter.
message IBinaryWriter {
    string name = 1;
    int32 level = 2;
}

message Container {
    IBinaryWriter writer = 1;
}
