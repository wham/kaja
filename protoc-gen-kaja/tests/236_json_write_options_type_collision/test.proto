syntax = "proto3";
package google.protobuf;

// A Timestamp represents a point in time.
message Timestamp {
    int64 seconds = 1;
    int32 nanos = 2;
}

// This message name collides with the JsonWriteOptions TYPE import.
message JsonWriteOptions {
    string format = 1;
    bool pretty = 2;
}
